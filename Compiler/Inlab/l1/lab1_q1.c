#include <stdio.h>
#include <string.h>

int main(int argc, char* argv[]){

	if(argc<2)
		exit(0);

	char* str = argv[1];
	int len = strlen(str);
	int i =0;
	char* array[]
	for(i=0;i<len;i++){
		int j=0;
		for(j=i;j<len;j++){

		}
	}
	
	return 0;
}